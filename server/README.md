To Run:

```
1. go to ura website and sign up for account.
2. get ura api key through email
3. create .env file at ROOT folder (SC2006_PropertyIQ)
4. Put in the key there (e.g. URA_API_KEY = 'INSERT API KEY HERE')
5. Run pip install -r requirements.txt in the command line 
6. run python file, json file should come out
7. go to google console and put ur credit card, won't get charged, got about 300+ free credits.
8. enable the API Key for google maps on google console
9. put the API key in line 27 of test.html and the google map should pop up
```